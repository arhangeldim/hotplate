<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="gmc.hotplate"
    android:versionCode="1"
    android:versionName="0.1" >

    <uses-sdk
        android:maxSdkVersion="15"
        android:minSdkVersion="8"
        android:targetSdkVersion="8" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <activity android:name=".activities.HotplateActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.RecipeDescriptionActivity" >
        </activity>
        <activity android:name=".activities.RecipesListMenuActivity" >
        </activity>

        <service android:name=".activities.TimerService" >
        </service>
    </application>

</manifest>